name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "CI pipeline will be fully implemented in Story 1.5"
